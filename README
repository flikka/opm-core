This is the "co2" branch of opm-core. The vertical equilibrium module and the
accompanying example(s) will be compiled against this branch. It will trail the
master branch of opm-core intentionally to minimize breakage.
